# تعليمات الوكيل اليومي — «قراءة اليوم من مجلة الرسالة»

أنت وكيل سحابي يعمل كل صباح. مهمتك: بناء **قراءة اليوم** من مجلة الرسالة، ودفعها إلى هذا المستودع
لتُنشر تلقائيًا عبر GitHub Pages على الرابط الثابت `pages_url` (في `progress.json`). اتبع هذه الخطوات حرفيًا.

## ١) اقرأ الحالة
- افتح `progress.json`: خذ `current_issue` و `next_article_index` و `articles_per_day`.
- افتح `data/issue-<NN>.json` للعدد الحالي (مثال `data/issue-01.json`) لمعرفة ترتيب المقالات وروابطها (`slug`).
- **إن لم يوجد ملف العدد**، ابنِه أولًا: اجلب فهرس العدد من ويكي مصدر
  (`https://ar.wikisource.org/wiki/مجلة_الرسالة/العدد_<N>`)، استخرج عناوين المقالات وروابطها الفرعية بالترتيب،
  واحفظها بنفس صيغة `data/issue-01.json`.

## ٢) اختر مقالات اليوم
- ابدأ من `next_article_index`، وخذ `articles_per_day` مقالة (أو أقل إن قارب العدد على الانتهاء).
- إن انتهى العدد الحالي: انتقل للعدد التالي، `next_article_index = 1`، وابنِ ملف فهرسه.

## ٣) اجلب النصّ الأصليّ (حرفيًّا — لا تختصره أبدًا)
لكل مقالة، اجلب النص الخام من ويكي مصدر:
```bash
curl -sL --get "https://ar.wikisource.org/w/index.php" \
  --data-urlencode "title=مجلة_الرسالة/العدد_<N>/<SLUG>" \
  --data-urlencode "action=raw" -H "User-Agent: Mozilla/5.0"
```
- استخرج اسم المؤلف من قالب `{{رأسية|مؤلف= ...}}`.
- انزع قوالب الويكي (`{{...}}`، `[[تصنيف:...]]`، الروابط `[[..|..]]`) وأبقِ **متن المقالة كاملًا كما كُتب** — دون تحديث ولا تصحيح إملائي للنصّ التاريخيّ.

## ٤) اكتب الشرح (هذا هو عملك الذكيّ)
لكل مقالة، أنشئ:
- **بطاقة «مَن الكاتب»**: سطران عن المؤلف وسياقه (اعتمد معرفتك؛ إن لم تعرفه، اذكر ما يظهر من النص فقط).
- **بطاقة «لماذا تستحقّ القراءة»**: لماذا تهمّ اليوم.
- **حواشٍ**: علّم الكلمات/العبارات الصعبة أو التاريخيّة داخل المتن بـ
  `<span class="g" data-note="الشرح هنا">الكلمة</span>` — الجافاسكربت في القالب يحوّلها تلقائيًا إلى حواشٍ مرقّمة.
  اشرح: الغريب اللغويّ، الإشارات التاريخيّة، الأعلام، التعابير القرآنيّة/التراثيّة. ٥–١٢ حاشية للمقالة الطويلة.
- التزم: **كل مصطلح تقنيّ عربيّ يليه مقابله الإنجليزيّ بين قوسين** عند أول ذكر (سياسة المستخدم الثابتة).

## ٥) ابنِ الصفحة
- انسخ `index.html` الحاليّ واستبدل **فقط** المناطق المحدَّدة بعلامات HTML:
  - `<!--META-->…<!--/META-->`  → «العدد N · التاريخ · القاهرة».
  - `<!--TODAY-->…<!--/TODAY-->` → وصف جرعة اليوم (مثال: «قراءة اليوم — مقالة: مصر في دورة الفلك»).
  - `<!--ARTICLES-->…<!--/ARTICLES-->` → مقالات اليوم، كلٌّ داخل `<article>` بنفس بنية المثال:
    رقم `.num` بالأرقام العربيّة، `h2` العنوان، `.by` السطر التعريفيّ، بطاقتا الشرح، ثمّ `.matn` بالمتن والحواشي، ثمّ `.signoff` و`.src` (رابط ويكي مصدر الكامل).
  - `<!--FOOTER-->…<!--/FOOTER-->` → «نهاية قراءة اليوم»، كم بقي من العدد، وتلميح لمقالة الغد.
  - عدّل تعليق `NEXT-POINTER` أعلى الملف.
- **لا تلمس** `<head>` ولا `<style>` ولا `<script>` — الثيم والحواشي التلقائيّة ثابتة.

## ٦) الأرشيف وحدّث الحالة
- احفظ نسخة في `archive/<YYYY-MM-DD>_issue<NN>_a<from>-<to>.html`.
- حدّث `progress.json`: زد `next_article_index`، وأضف مقالات اليوم إلى `done` بتاريخ اليوم (بصيغة YYYY-MM-DD؛ استخرجه بـ `date +%F`).

## ٧) ادفع
```bash
git add -A && git commit -m "قراءة اليوم: العدد <N> — <عناوين مقالات اليوم>" && git push
```
GitHub Pages ينشر التحديث خلال دقيقة على `pages_url`.

## مبادئ ثابتة
- **أمانة النصّ**: المتن الأصليّ كامل وحرفيّ. أيّ إضافة منك تكون في البطاقات أو الحواشي، لا في المتن.
- **جودة الحواشي**: تشرح فعلًا (معنى الكلمة، لا إعادة صياغتها). قصيرة ودقيقة.
- **الاتساق**: نفس الثيم والبنية كل يوم. لا تعِد اختراع التصميم.
- إن فشل جلب مقالة (لا نصّ/خطأ شبكة): تخطَّها إلى التالية وسجّل ذلك في رسالة الـ commit، ولا توقف اليوم.
