# كيف نضيف أيّام قراءة جديدة (تعبئة الرصيد)

النظام يعرض قراءة اليوم حسب التاريخ (اليوم ١ = 2026-08-22). عند اقتراب الرصيد من النفاد،
نضيف أيّامًا جديدة. الخطوات (تُنفَّذ محليًّا على جهاز المالك حيث يعمل الإنترنت وgit push):

1. النصوص الخام للعدد الأوّل جاهزة في `data/issue-01/raw/*.txt`. لعددٍ جديد اسحبها أوّلًا:
   ```bash
   curl -sL --get "https://ar.wikisource.org/w/index.php" \
     --data-urlencode "title=مجلة_الرسالة/العدد_<N>/<SLUG>" \
     --data-urlencode "action=raw" -H "User-Agent: Mozilla/5.0" -o "data/issue-XX/raw/<SLUG>.txt"
   ```
2. لكل يوم جديد، أنشئ `days/day-0NN.html` بنسخ بنية `days/day-002.html`:
   - المتن الأصليّ كاملًا حرفيًّا من ملفّ الخام (انزع قوالب الويكي).
   - بطاقتا «مَن الكاتب» و«لماذا تستحقّ القراءة».
   - علّم الكلمات الصعبة بـ `<span class="g" data-note="الشرح">الكلمة</span>` (تتحوّل لحواشٍ تلقائيًّا).
   - مصطلح تقنيّ عربيّ ← يليه الإنجليزيّ بين قوسين.
   - اضبط روابط التنقّل (السابق/التالي) في الأعلى.
3. حدّث `manifest.json`: زِد `total`، أضِف عنصرًا في `days`، وحدّث `next_article_index`.
4. عدّل «التالي ▶» في اليوم السابق ليشير للجديد.
5. `git add -A && git commit -m "أيام جديدة: ..." && git push` — GitHub Pages ينشر خلال دقيقة.

الثيم والحواشي في `assets/` — لا تكرّرها في كل صفحة.
