# مشروع «الرسالة» — كتاب قراءة إلكتروني لمجلة الرسالة (١٩٣٣)

توثيق شامل للمشروع كي تعود إليه أنت أو أيّ جلسة Claude لاحقة. **اقرأ هذا الملف أولًا.**

## الرابط الحيّ
👉 **https://almatrouk88.github.io/risala-daily/**  (تُضاف للشاشة الرئيسية كتطبيق)

## ما هو
موقع قراءة عربيّ RTL لأعداد مجلة الرسالة من أرشيف [ويكي مصدر](https://ar.wikisource.org/wiki/مجلة_الرسالة)،
مصمَّم للجوّال وأجهزة الحبر الإلكترونيّ (BOOX): المتن الأصليّ كامل + بطاقتا تعريف + حواشٍ للكلمات الصعبة.

## أين كل شيء
- **مجلد العمل / مستودع git:** `~/risala-daily` (هو المصدر الوحيد للحقيقة).
- **مستودع GitHub (عام):** `github.com/almatrouk88/risala-daily` — يُنشر عبر **GitHub Pages** (فرع main، الجذر).
- **مجلد سهل الرجوع:** `~/رسالة` (فيه دليل + وصلة لهذا المستودع).

## البنية
```
index.html        ← الرئيسية: زرّ متابعة + استعادة سحابية + فهرس الأعداد
archive.html      ← قائمة الأعداد
issue.html?n=N    ← مقالات عدد معيّن
search.html       ← بحث عادي (اسم/كاتب) + متقدّم (سنة/عدد)
marks.html        ← العلامات المرجعية + التظليلات
days/day-0NN.html ← صفحة يوم (مقالة أو مقالتان)
assets/style.css  ← الثيم (Amiri مدمج في assets/fonts/)
assets/footnotes.js ← محرّك القارئ (حواشٍ، وضع كتاب، تحكّم، مزامنة)
index.json        ← فهرس المقالات + الأعداد (للبحث والتصفّح)
manifest.json     ← start/total للتدوير بالتاريخ
data/issue-01/raw/*.txt ← النصوص الخام الأصلية (ملكية عامة)
REFILL.md         ← كيف تُضاف أيّام/أعداد جديدة
```

## القرارات المهمّة (لماذا هكذا)
1. **لا أتمتة سحابيّة للبناء:** جُرِّب routine سحابيّ وفشل — البيئة السحابيّة تحجب wikisource، ودفع git يتطلّب خطة Team. الحلّ: **البناء محليّ (Claude في جلسة) + النشر عبر Pages**. العرض بالتاريخ تلقائيّ، لكن بناء المحتوى الجديد يدويّ.
2. **النصوص تُجهَّز مسبقًا محليًّا** في `data/issue-XX/raw/` (لأن السحابة محجوبة).
3. **الأمانة للنصّ:** المتن حرفيّ كامل — رُوجعت كل مقالة بمقارنة عدد الكلمات مع الخام (~١.٠٠).
4. **تصميم الحبر:** أبيض/أسود، كثافة عالية (line-height 1.75)، **وضع الكتاب** (أعمدة، ارتفاع بالبكسل، بلا قصّ أسطر)، لمس يسار/يمين للتقليب، أدوات مخفيّة تظهر بلمس الوسط.
5. **المزامنة بين الأجهزة (يدويّة):** أزرار ☁↑/☁↓ عبر kvdb.io — **الدلو:** `Hj8v3hbdFx6wBP8hrRyaUk`، المفتاح `risala_pos`. يتطلّب تفعيل بريد المالك مرّة واحدة على kvdb.io.

## الحالة (٢٤ أغسطس ٢٠٢٦)
- **العدد الأول كامل: ١٩ مقالة في ١٠ أيام** (day-001..day-010).
- **التالي:** العدد الثاني — يحتاج سحب نصوصه الخام ثم بناء أيّام day-011+.

## كيف تُكمل (العدد التالي)
1. اسحب فهرس العدد ونصوصه الخام من ويكي مصدر (انظر REFILL.md، والأمر `action=raw`).
2. ابنِ صفحات الأيّام بنسخ `days/day-002.html`، مقالتان/يوم، بمعرّفات `d{day}-a{idx}`.
3. حدّث `index.json` (issues + articles) و`manifest.json` (total)، وفعّل رابط «التالي» في آخر يوم سابق.
4. تحقّق (توازن الوسوم + مقارنة كلمات المتن بالخام)، ثم `git push`.

## الصلة
هذا المشروع هو **التطبيق المرجعيّ** لمهارة `magazine-reader` في `~/.claude/skills/` — تبني نفس النظام لأيّ مجلة أخرى.
